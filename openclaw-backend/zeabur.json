{
  "$schema": "https://schema.zeabur.com/config.json",
  "build": {
    "command": "npm install",
    "output": "."
  },
  "startCommand": "npx prisma generate && npx prisma db push && npm run db:seed && npx tsc && node dist/index.js"
}
